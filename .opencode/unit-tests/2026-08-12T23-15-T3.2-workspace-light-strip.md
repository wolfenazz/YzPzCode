# Unit Test Record: T3.2 Strip light branches from workspace components

## Target Files
- `app/src/components/workspace/TerminalPane.tsx`
- `app/src/components/workspace/TerminalHeader.tsx`
- `app/src/components/workspace/TerminalGrid.tsx`
- `app/src/components/workspace/NewTerminalDialog.tsx`
- `app/src/components/workspace/CliStatusBadge.tsx`
- `app/src/components/workspace/AuthModal.tsx`
- `app/src/components/workspace/SortableTerminalPane.tsx`
- `app/src/components/workspace/BrowserPane.tsx`
- `app/src/components/workspace/TerminalStatusBar.tsx`
- `app/src/components/workspace/Workspace.tsx`
- `app/src/components/editor/FindReplaceBar.tsx` (dead code, theme prop stripped)
- `app/src/stores/appStore.ts` (theme type locked to "dark")

## Test File (DELETED)
N/A — CSS/JSX static-structure change; verified by TypeScript compile + grep, no isolated test file
created (project has no frontend test framework; verification = `npx tsc --noEmit` + remnant grep).

## Verification Evidence
- `npx tsc --noEmit` → TSC_EXIT=0 (zero type errors, full project)
- Grep `isLight` (excluding designerGenerator.ts — designer theme-ID feature, out of scope):
  → ZERO matches in workspace components
- Grep `theme: 'dark' | 'light'` / `theme?: 'dark' | 'light'` → ZERO matches
- Grep `toggleTheme` / `onThemeToggle` / `light-theme` / `markdown-light` / `docx-preview-light`
  / `spreadsheet-light` → ZERO matches in app/src
- TerminalPane `LIGHT_TERMINAL_THEME` deleted; `terminalTheme = DARK_TERMINAL_THEME` (always dark)
- TerminalPane container bg = `bg-[#262626]` (resolves SYNC-2 MEDIUM)
- `npm run build` → pending (job_f24a98d8)

## Changes Made
- Removed `theme` prop from TerminalPaneProps/TerminalGridProps/NewTerminalDialogProps/
  CliStatusBadgeProps/BrowserPaneProps/SortableTerminalPaneProps/TerminalHeaderProps/FindReplaceBarProps
- Removed `theme`/`isLight` from all component destructuring + `const isLight = theme === 'light'`
- Collapsed every `isLight ? 'X' : 'Y'` ternary to its dark branch ('Y')
- Removed `theme={theme}` prop passes at all call sites (Workspace → TerminalGrid/BrowserPane,
  TerminalGrid → SortableTerminalPane/NewTerminalDialog, TerminalPane → TerminalHeader/
  CliStatusBadge/AuthModal, TerminalStatusBar → NewTerminalDialog, TerminalHeader → QuickActions)
- Removed `theme` from Workspace.tsx store destructure + TerminalStatusBar store selector
- Deleted `LIGHT_TERMINAL_THEME` const from TerminalPane
- appStore: `theme: "dark" | "light"` → `theme: "dark"` (locked)

## Test Result
- Status: pass (tsc 0; greps 0; build running)
- Session: ses_00876light
- Timestamp: 2026-08-12T23:15
