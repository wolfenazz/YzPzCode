# Unit Test Record — branding.ts (M4 T4.1 "Tighten agent system prompt")

- **Date:** 2026-08-14
- **Reviewer:** ses (Unit Review)
- **Module under test:** `app/agent-harness/src/branding.ts` (+ built `dist/branding.js`)
- **Related tasks:** S4.1.1 (EFFICIENCY_DIRECTIVE rules), S4.1.2 (typecheck + build)

## S4.1.1 — Prompt rules (static verification)

All three required rules present in `EFFICIENCY_DIRECTIVE`, each exactly once (no SYNC-1-style dupes):

| Rule | branding.ts line | Present |
|------|------------------|---------|
| verify file existence before read ("Before reading a file, confirm it exists first… Never read a guessed path") | 31 | ✅ (count=1) |
| 1-3 sentence final summary + bullets ("Keep the final summary to 1-3 short sentences plus a compact bullet list…") | 32 | ✅ (count=1) |
| no step-by-step narration ("Do not announce your steps ('let me explore…', 'let me check…')…") | 33 | ✅ (count=1) |

Regression guards also verified exactly-once: "stop gathering and answer immediately" (:24), "read only the relevant region" (:25).

## S4.1.2 — Build gates

| Gate | Command | Result |
|------|---------|--------|
| Typecheck | `npm run typecheck` (tsc --noEmit, agent-harness) | EXIT 0, zero errors |
| Build | `npm run build` (tsc → dist/) | EXIT 0, dist/branding.js emitted |

## Functional unit test — emitted prompt content (probe, 14/14 PASS, EXIT 0)

Ran `buildSystemPrompt(null, "<workspace>")` against `dist/branding.js`:

1. source rule exactly once: "stop gathering and answer immediately" — ok
2. source rule exactly once: "confirm it exists first" — ok
3. source rule exactly once: "final summary to 1-3 short sentences" — ok
4. source rule exactly once: "Do not announce your steps" — ok
5. source rule exactly once: "read only the relevant region" — ok
6. functional: S4.1.1 rule verify file exists before read — ok
7. functional: S4.1.1 rule 1-3 sentence final summary + bullets — ok
8. functional: S4.1.1 rule no step-by-step narration — ok
9. functional: stop gathering when enough context — ok
10. functional: workspace root injected (`C:/Users/nasee/Desktop/files/CODING/yzpzcode`) — ok
11. functional: workspace resolution directive — ok
12. functional: TOOL_SNIPPETS present — ok
13. functional: YZPZ branding present — ok
14. functional: no "undefined" leakage — ok

Emitted prompt length: 7636 chars.

## Code quality / modularity

- Single-responsibility module (70 lines): constants (`YZPZ_BRANDING`, `EFFICIENCY_DIRECTIVE`, `TOOL_SNIPPETS`) separated from the `buildSystemPrompt` builder function. ✅
- No debug logging (only intentional `console.warn` in the `getClineDefaultSystemPrompt` failure fallback). ✅
- No hardcoded secrets. ✅
- New `workspaceRoot` param properly wired at harness.ts:652 (`buildSystemPrompt(args.systemPrompt, args.cwd)`). ✅

## Result: PASS ✅ — zero defects. Unit ready for integration (M4 complete).
