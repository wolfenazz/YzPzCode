# Sync Issues (Unresolved Only)

_(None — all resolved. Independent re-verification 2026-08-14T00:58Z: all structural checks pass, all gates EXIT 0, TODO 12/12 [x].)_

_Note (non-blocking, informational): `app/agent-harness/src/harness.ts` (modified) and `app/agent-harness/probe-cwd.mjs` (untracked) are additional files beyond the 4 intended change-set files — they implement the read_files cwd-resolution fix coupled to the branding.ts `buildSystemPrompt` workspaceRoot param (M4). Both verified passing (probe-cwd.mjs 4/4 EXIT 0, typecheck/build EXIT 0). Not a regression._
