# Copy to .env (never commit real values). Backend reads DISCORD_WEBHOOK_URL at runtime.
# Create a new Discord webhook: Server Settings → Integrations → Webhooks → New Webhook → Copy URL
DISCORD_WEBHOOK_URL=
